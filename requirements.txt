# RAG 핵심 라이브러리
chromadb==0.5.3
openai==1.35.0
# openai 1.35.x는 httpx 0.28.x와 proxies 인자 호환 문제가 있어 0.27.x로 고정
httpx==0.27.2

# UI
streamlit==1.36.0

# 로컬 문서 업로드/파싱
pypdf==4.3.1
python-docx==1.1.2

# 환경 변수
python-dotenv==1.0.1

# 유틸리티
tqdm==4.66.4
pandas==2.2.2

# 선택 사항: OPENAI_API_KEY가 없을 때 로컬 임베딩 fallback용
sentence-transformers==3.0.1
